#include "main.h"

/**
 * _strlen - calculates the length of a string
 * @s: pointer to variable
 *
 * Return: total
 */
int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
