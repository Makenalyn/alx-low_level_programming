#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: name of the arguments
 *
 * Return: program name
 */
int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
